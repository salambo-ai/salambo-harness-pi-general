# Workspace Seed

This folder is copied into `/workspace/work` when the sandbox image seeds a new workspace.

Useful starter locations:

- `files/` for uploaded or preloaded inputs
- `templates/` for starter assets and prompts
