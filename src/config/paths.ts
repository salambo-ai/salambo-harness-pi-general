import path from 'path';

export const PROJECT_ROOT = process.cwd();
export const DEFAULT_WORKSPACE_DIR = '/workspace';
export const DEFAULT_PI_HOME = path.resolve(PROJECT_ROOT, 'harness-config', 'pi-agent-home');
