# Pi Themes

Template-level pi themes live here.

Pi loads themes from `PI_HOME/themes/`, so files in this directory become part of the seeded runtime defaults for the sandbox image.

Use this for themes that should ship with the template itself.

For project-specific themes, use the workspace-local `.pi/themes/` directory instead.
