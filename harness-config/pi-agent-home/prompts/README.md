# Pi Prompt Templates

Template-level pi prompt templates live here.

Pi loads prompt templates from `PI_HOME/prompts/`, so files in this directory become part of the seeded runtime defaults for the sandbox image.

Use this for prompts that should ship with the template itself.

For project-specific prompts, use the workspace-local `.pi/prompts/` directory instead.
