# Pi Skills

Template-level pi skills live here.

Pi loads skills from `PI_HOME/skills/`, so anything placed in this directory becomes part of the seeded runtime defaults for the sandbox image.

Use this for skills that should ship with the template itself.

For project-specific skills, use the workspace-local `.pi/skills/` directory instead.
