# Project Pi Overrides

This directory is copied into the sandbox workspace as `/workspace/.pi/`.

Pi treats `.pi/` in the working directory as the project-local override layer. Use it for repo-specific settings, extensions, skills, prompt templates, and themes.

Template defaults should live in `harness-config/pi-agent-home/`.
